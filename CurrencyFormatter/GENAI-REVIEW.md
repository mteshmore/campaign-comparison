I noticed that for negative CAD values, the currency code wasn’t always displayed by `Intl.NumberFormat`; it sometimes output just `-$98,765.43` instead of `-CA$98,765.43`. This inconsistency is due to how the formatter handles certain locales in Node.js. A more reliable approach could be to manually map the currency code for CAD, USD, and other currencies, ensuring that the correct prefix always appears in the output.